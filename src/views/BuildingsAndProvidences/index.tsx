import React from "react";
import { observer } from "mobx-react-lite";

const BuildingsAndProvidences: React.FC<{}> = observer(() => {
  return <div>Obras e providências</div>;
});

export default BuildingsAndProvidences;
